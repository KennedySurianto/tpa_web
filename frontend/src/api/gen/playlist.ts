// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.7.0
//   protoc               v6.31.0
// source: playlist.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { grpc } from "@improbable-eng/grpc-web";
import { BrowserHeaders } from "browser-headers";
import { Timestamp } from "./google/protobuf/timestamp";

export const protobufPackage = "playlist";

export interface CreatePlaylistRequest {
  name: string;
  userId: string;
  videoIds: string[];
}

export interface CreatePlaylistResponse {
  playlistId: string;
}

export interface GetPlaylistRequest {
  id: string;
}

export interface User {
  id: string;
  username: string;
  avatar: Uint8Array;
}

export interface Video {
  id: number;
  createdAt?: Date | undefined;
  updatedAt?: Date | undefined;
  deletedAt?:
    | Date
    | undefined;
  /** Video metadata */
  userId: number;
  videoUrl: string;
  thumbnail: Uint8Array;
  caption: string;
  description?: string | undefined;
  duration: number;
  /** Optional associations */
  soundId?: number | undefined;
  privacy: string;
  /** Cached metrics */
  viewsCount: number;
  likesCount: number;
  commentsCount: number;
  /** Feature flags */
  allowComments: boolean;
  allowDuet: boolean;
  allowStitch: boolean;
  user?: User | undefined;
  isLiked: boolean;
  likeCount: string;
}

export interface Playlist {
  id: string;
  name: string;
  userId: string;
  videos: Video[];
  createdAt?: Date | undefined;
  updatedAt?: Date | undefined;
  deletedAt?: Date | undefined;
}

export interface GetPlaylistByUserIdResponse {
  playlists: Playlist[];
}

export interface DeletePlaylistRequest {
  id: string;
}

export interface DeletePlaylistResponse {
  success: boolean;
}

function createBaseCreatePlaylistRequest(): CreatePlaylistRequest {
  return { name: "", userId: "0", videoIds: [] };
}

export const CreatePlaylistRequest: MessageFns<CreatePlaylistRequest> = {
  encode(message: CreatePlaylistRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.name !== "") {
      writer.uint32(10).string(message.name);
    }
    if (message.userId !== "0") {
      writer.uint32(16).uint64(message.userId);
    }
    writer.uint32(26).fork();
    for (const v of message.videoIds) {
      writer.uint64(v);
    }
    writer.join();
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): CreatePlaylistRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseCreatePlaylistRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.name = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.userId = reader.uint64().toString();
          continue;
        }
        case 3: {
          if (tag === 24) {
            message.videoIds.push(reader.uint64().toString());

            continue;
          }

          if (tag === 26) {
            const end2 = reader.uint32() + reader.pos;
            while (reader.pos < end2) {
              message.videoIds.push(reader.uint64().toString());
            }

            continue;
          }

          break;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): CreatePlaylistRequest {
    return {
      name: isSet(object.name) ? globalThis.String(object.name) : "",
      userId: isSet(object.userId) ? globalThis.String(object.userId) : "0",
      videoIds: globalThis.Array.isArray(object?.videoIds) ? object.videoIds.map((e: any) => globalThis.String(e)) : [],
    };
  },

  toJSON(message: CreatePlaylistRequest): unknown {
    const obj: any = {};
    if (message.name !== "") {
      obj.name = message.name;
    }
    if (message.userId !== "0") {
      obj.userId = message.userId;
    }
    if (message.videoIds?.length) {
      obj.videoIds = message.videoIds;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<CreatePlaylistRequest>, I>>(base?: I): CreatePlaylistRequest {
    return CreatePlaylistRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<CreatePlaylistRequest>, I>>(object: I): CreatePlaylistRequest {
    const message = createBaseCreatePlaylistRequest();
    message.name = object.name ?? "";
    message.userId = object.userId ?? "0";
    message.videoIds = object.videoIds?.map((e) => e) || [];
    return message;
  },
};

function createBaseCreatePlaylistResponse(): CreatePlaylistResponse {
  return { playlistId: "0" };
}

export const CreatePlaylistResponse: MessageFns<CreatePlaylistResponse> = {
  encode(message: CreatePlaylistResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.playlistId !== "0") {
      writer.uint32(8).uint64(message.playlistId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): CreatePlaylistResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseCreatePlaylistResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.playlistId = reader.uint64().toString();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): CreatePlaylistResponse {
    return { playlistId: isSet(object.playlistId) ? globalThis.String(object.playlistId) : "0" };
  },

  toJSON(message: CreatePlaylistResponse): unknown {
    const obj: any = {};
    if (message.playlistId !== "0") {
      obj.playlistId = message.playlistId;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<CreatePlaylistResponse>, I>>(base?: I): CreatePlaylistResponse {
    return CreatePlaylistResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<CreatePlaylistResponse>, I>>(object: I): CreatePlaylistResponse {
    const message = createBaseCreatePlaylistResponse();
    message.playlistId = object.playlistId ?? "0";
    return message;
  },
};

function createBaseGetPlaylistRequest(): GetPlaylistRequest {
  return { id: "0" };
}

export const GetPlaylistRequest: MessageFns<GetPlaylistRequest> = {
  encode(message: GetPlaylistRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.id !== "0") {
      writer.uint32(8).uint64(message.id);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetPlaylistRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetPlaylistRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.id = reader.uint64().toString();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GetPlaylistRequest {
    return { id: isSet(object.id) ? globalThis.String(object.id) : "0" };
  },

  toJSON(message: GetPlaylistRequest): unknown {
    const obj: any = {};
    if (message.id !== "0") {
      obj.id = message.id;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GetPlaylistRequest>, I>>(base?: I): GetPlaylistRequest {
    return GetPlaylistRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GetPlaylistRequest>, I>>(object: I): GetPlaylistRequest {
    const message = createBaseGetPlaylistRequest();
    message.id = object.id ?? "0";
    return message;
  },
};

function createBaseUser(): User {
  return { id: "0", username: "", avatar: new Uint8Array(0) };
}

export const User: MessageFns<User> = {
  encode(message: User, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.id !== "0") {
      writer.uint32(8).uint64(message.id);
    }
    if (message.username !== "") {
      writer.uint32(18).string(message.username);
    }
    if (message.avatar.length !== 0) {
      writer.uint32(26).bytes(message.avatar);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): User {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseUser();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.id = reader.uint64().toString();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.username = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.avatar = reader.bytes();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): User {
    return {
      id: isSet(object.id) ? globalThis.String(object.id) : "0",
      username: isSet(object.username) ? globalThis.String(object.username) : "",
      avatar: isSet(object.avatar) ? bytesFromBase64(object.avatar) : new Uint8Array(0),
    };
  },

  toJSON(message: User): unknown {
    const obj: any = {};
    if (message.id !== "0") {
      obj.id = message.id;
    }
    if (message.username !== "") {
      obj.username = message.username;
    }
    if (message.avatar.length !== 0) {
      obj.avatar = base64FromBytes(message.avatar);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<User>, I>>(base?: I): User {
    return User.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<User>, I>>(object: I): User {
    const message = createBaseUser();
    message.id = object.id ?? "0";
    message.username = object.username ?? "";
    message.avatar = object.avatar ?? new Uint8Array(0);
    return message;
  },
};

function createBaseVideo(): Video {
  return {
    id: 0,
    createdAt: undefined,
    updatedAt: undefined,
    deletedAt: undefined,
    userId: 0,
    videoUrl: "",
    thumbnail: new Uint8Array(0),
    caption: "",
    description: undefined,
    duration: 0,
    soundId: undefined,
    privacy: "",
    viewsCount: 0,
    likesCount: 0,
    commentsCount: 0,
    allowComments: false,
    allowDuet: false,
    allowStitch: false,
    user: undefined,
    isLiked: false,
    likeCount: "0",
  };
}

export const Video: MessageFns<Video> = {
  encode(message: Video, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.id !== 0) {
      writer.uint32(8).uint32(message.id);
    }
    if (message.createdAt !== undefined) {
      Timestamp.encode(toTimestamp(message.createdAt), writer.uint32(18).fork()).join();
    }
    if (message.updatedAt !== undefined) {
      Timestamp.encode(toTimestamp(message.updatedAt), writer.uint32(26).fork()).join();
    }
    if (message.deletedAt !== undefined) {
      Timestamp.encode(toTimestamp(message.deletedAt), writer.uint32(34).fork()).join();
    }
    if (message.userId !== 0) {
      writer.uint32(40).uint32(message.userId);
    }
    if (message.videoUrl !== "") {
      writer.uint32(50).string(message.videoUrl);
    }
    if (message.thumbnail.length !== 0) {
      writer.uint32(58).bytes(message.thumbnail);
    }
    if (message.caption !== "") {
      writer.uint32(66).string(message.caption);
    }
    if (message.description !== undefined) {
      writer.uint32(74).string(message.description);
    }
    if (message.duration !== 0) {
      writer.uint32(80).int32(message.duration);
    }
    if (message.soundId !== undefined) {
      writer.uint32(88).uint32(message.soundId);
    }
    if (message.privacy !== "") {
      writer.uint32(98).string(message.privacy);
    }
    if (message.viewsCount !== 0) {
      writer.uint32(104).uint32(message.viewsCount);
    }
    if (message.likesCount !== 0) {
      writer.uint32(112).uint32(message.likesCount);
    }
    if (message.commentsCount !== 0) {
      writer.uint32(120).uint32(message.commentsCount);
    }
    if (message.allowComments !== false) {
      writer.uint32(128).bool(message.allowComments);
    }
    if (message.allowDuet !== false) {
      writer.uint32(136).bool(message.allowDuet);
    }
    if (message.allowStitch !== false) {
      writer.uint32(144).bool(message.allowStitch);
    }
    if (message.user !== undefined) {
      User.encode(message.user, writer.uint32(154).fork()).join();
    }
    if (message.isLiked !== false) {
      writer.uint32(160).bool(message.isLiked);
    }
    if (message.likeCount !== "0") {
      writer.uint32(168).uint64(message.likeCount);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Video {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseVideo();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.id = reader.uint32();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.createdAt = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.updatedAt = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.deletedAt = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.userId = reader.uint32();
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.videoUrl = reader.string();
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.thumbnail = reader.bytes();
          continue;
        }
        case 8: {
          if (tag !== 66) {
            break;
          }

          message.caption = reader.string();
          continue;
        }
        case 9: {
          if (tag !== 74) {
            break;
          }

          message.description = reader.string();
          continue;
        }
        case 10: {
          if (tag !== 80) {
            break;
          }

          message.duration = reader.int32();
          continue;
        }
        case 11: {
          if (tag !== 88) {
            break;
          }

          message.soundId = reader.uint32();
          continue;
        }
        case 12: {
          if (tag !== 98) {
            break;
          }

          message.privacy = reader.string();
          continue;
        }
        case 13: {
          if (tag !== 104) {
            break;
          }

          message.viewsCount = reader.uint32();
          continue;
        }
        case 14: {
          if (tag !== 112) {
            break;
          }

          message.likesCount = reader.uint32();
          continue;
        }
        case 15: {
          if (tag !== 120) {
            break;
          }

          message.commentsCount = reader.uint32();
          continue;
        }
        case 16: {
          if (tag !== 128) {
            break;
          }

          message.allowComments = reader.bool();
          continue;
        }
        case 17: {
          if (tag !== 136) {
            break;
          }

          message.allowDuet = reader.bool();
          continue;
        }
        case 18: {
          if (tag !== 144) {
            break;
          }

          message.allowStitch = reader.bool();
          continue;
        }
        case 19: {
          if (tag !== 154) {
            break;
          }

          message.user = User.decode(reader, reader.uint32());
          continue;
        }
        case 20: {
          if (tag !== 160) {
            break;
          }

          message.isLiked = reader.bool();
          continue;
        }
        case 21: {
          if (tag !== 168) {
            break;
          }

          message.likeCount = reader.uint64().toString();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Video {
    return {
      id: isSet(object.id) ? globalThis.Number(object.id) : 0,
      createdAt: isSet(object.createdAt) ? fromJsonTimestamp(object.createdAt) : undefined,
      updatedAt: isSet(object.updatedAt) ? fromJsonTimestamp(object.updatedAt) : undefined,
      deletedAt: isSet(object.deletedAt) ? fromJsonTimestamp(object.deletedAt) : undefined,
      userId: isSet(object.userId) ? globalThis.Number(object.userId) : 0,
      videoUrl: isSet(object.videoUrl) ? globalThis.String(object.videoUrl) : "",
      thumbnail: isSet(object.thumbnail) ? bytesFromBase64(object.thumbnail) : new Uint8Array(0),
      caption: isSet(object.caption) ? globalThis.String(object.caption) : "",
      description: isSet(object.description) ? globalThis.String(object.description) : undefined,
      duration: isSet(object.duration) ? globalThis.Number(object.duration) : 0,
      soundId: isSet(object.soundId) ? globalThis.Number(object.soundId) : undefined,
      privacy: isSet(object.privacy) ? globalThis.String(object.privacy) : "",
      viewsCount: isSet(object.viewsCount) ? globalThis.Number(object.viewsCount) : 0,
      likesCount: isSet(object.likesCount) ? globalThis.Number(object.likesCount) : 0,
      commentsCount: isSet(object.commentsCount) ? globalThis.Number(object.commentsCount) : 0,
      allowComments: isSet(object.allowComments) ? globalThis.Boolean(object.allowComments) : false,
      allowDuet: isSet(object.allowDuet) ? globalThis.Boolean(object.allowDuet) : false,
      allowStitch: isSet(object.allowStitch) ? globalThis.Boolean(object.allowStitch) : false,
      user: isSet(object.user) ? User.fromJSON(object.user) : undefined,
      isLiked: isSet(object.isLiked) ? globalThis.Boolean(object.isLiked) : false,
      likeCount: isSet(object.likeCount) ? globalThis.String(object.likeCount) : "0",
    };
  },

  toJSON(message: Video): unknown {
    const obj: any = {};
    if (message.id !== 0) {
      obj.id = Math.round(message.id);
    }
    if (message.createdAt !== undefined) {
      obj.createdAt = message.createdAt.toISOString();
    }
    if (message.updatedAt !== undefined) {
      obj.updatedAt = message.updatedAt.toISOString();
    }
    if (message.deletedAt !== undefined) {
      obj.deletedAt = message.deletedAt.toISOString();
    }
    if (message.userId !== 0) {
      obj.userId = Math.round(message.userId);
    }
    if (message.videoUrl !== "") {
      obj.videoUrl = message.videoUrl;
    }
    if (message.thumbnail.length !== 0) {
      obj.thumbnail = base64FromBytes(message.thumbnail);
    }
    if (message.caption !== "") {
      obj.caption = message.caption;
    }
    if (message.description !== undefined) {
      obj.description = message.description;
    }
    if (message.duration !== 0) {
      obj.duration = Math.round(message.duration);
    }
    if (message.soundId !== undefined) {
      obj.soundId = Math.round(message.soundId);
    }
    if (message.privacy !== "") {
      obj.privacy = message.privacy;
    }
    if (message.viewsCount !== 0) {
      obj.viewsCount = Math.round(message.viewsCount);
    }
    if (message.likesCount !== 0) {
      obj.likesCount = Math.round(message.likesCount);
    }
    if (message.commentsCount !== 0) {
      obj.commentsCount = Math.round(message.commentsCount);
    }
    if (message.allowComments !== false) {
      obj.allowComments = message.allowComments;
    }
    if (message.allowDuet !== false) {
      obj.allowDuet = message.allowDuet;
    }
    if (message.allowStitch !== false) {
      obj.allowStitch = message.allowStitch;
    }
    if (message.user !== undefined) {
      obj.user = User.toJSON(message.user);
    }
    if (message.isLiked !== false) {
      obj.isLiked = message.isLiked;
    }
    if (message.likeCount !== "0") {
      obj.likeCount = message.likeCount;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<Video>, I>>(base?: I): Video {
    return Video.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Video>, I>>(object: I): Video {
    const message = createBaseVideo();
    message.id = object.id ?? 0;
    message.createdAt = object.createdAt ?? undefined;
    message.updatedAt = object.updatedAt ?? undefined;
    message.deletedAt = object.deletedAt ?? undefined;
    message.userId = object.userId ?? 0;
    message.videoUrl = object.videoUrl ?? "";
    message.thumbnail = object.thumbnail ?? new Uint8Array(0);
    message.caption = object.caption ?? "";
    message.description = object.description ?? undefined;
    message.duration = object.duration ?? 0;
    message.soundId = object.soundId ?? undefined;
    message.privacy = object.privacy ?? "";
    message.viewsCount = object.viewsCount ?? 0;
    message.likesCount = object.likesCount ?? 0;
    message.commentsCount = object.commentsCount ?? 0;
    message.allowComments = object.allowComments ?? false;
    message.allowDuet = object.allowDuet ?? false;
    message.allowStitch = object.allowStitch ?? false;
    message.user = (object.user !== undefined && object.user !== null) ? User.fromPartial(object.user) : undefined;
    message.isLiked = object.isLiked ?? false;
    message.likeCount = object.likeCount ?? "0";
    return message;
  },
};

function createBasePlaylist(): Playlist {
  return {
    id: "0",
    name: "",
    userId: "0",
    videos: [],
    createdAt: undefined,
    updatedAt: undefined,
    deletedAt: undefined,
  };
}

export const Playlist: MessageFns<Playlist> = {
  encode(message: Playlist, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.id !== "0") {
      writer.uint32(8).uint64(message.id);
    }
    if (message.name !== "") {
      writer.uint32(18).string(message.name);
    }
    if (message.userId !== "0") {
      writer.uint32(24).uint64(message.userId);
    }
    for (const v of message.videos) {
      Video.encode(v!, writer.uint32(34).fork()).join();
    }
    if (message.createdAt !== undefined) {
      Timestamp.encode(toTimestamp(message.createdAt), writer.uint32(42).fork()).join();
    }
    if (message.updatedAt !== undefined) {
      Timestamp.encode(toTimestamp(message.updatedAt), writer.uint32(50).fork()).join();
    }
    if (message.deletedAt !== undefined) {
      Timestamp.encode(toTimestamp(message.deletedAt), writer.uint32(58).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Playlist {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePlaylist();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.id = reader.uint64().toString();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.name = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.userId = reader.uint64().toString();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.videos.push(Video.decode(reader, reader.uint32()));
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.createdAt = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.updatedAt = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.deletedAt = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Playlist {
    return {
      id: isSet(object.id) ? globalThis.String(object.id) : "0",
      name: isSet(object.name) ? globalThis.String(object.name) : "",
      userId: isSet(object.userId) ? globalThis.String(object.userId) : "0",
      videos: globalThis.Array.isArray(object?.videos) ? object.videos.map((e: any) => Video.fromJSON(e)) : [],
      createdAt: isSet(object.createdAt) ? fromJsonTimestamp(object.createdAt) : undefined,
      updatedAt: isSet(object.updatedAt) ? fromJsonTimestamp(object.updatedAt) : undefined,
      deletedAt: isSet(object.deletedAt) ? fromJsonTimestamp(object.deletedAt) : undefined,
    };
  },

  toJSON(message: Playlist): unknown {
    const obj: any = {};
    if (message.id !== "0") {
      obj.id = message.id;
    }
    if (message.name !== "") {
      obj.name = message.name;
    }
    if (message.userId !== "0") {
      obj.userId = message.userId;
    }
    if (message.videos?.length) {
      obj.videos = message.videos.map((e) => Video.toJSON(e));
    }
    if (message.createdAt !== undefined) {
      obj.createdAt = message.createdAt.toISOString();
    }
    if (message.updatedAt !== undefined) {
      obj.updatedAt = message.updatedAt.toISOString();
    }
    if (message.deletedAt !== undefined) {
      obj.deletedAt = message.deletedAt.toISOString();
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<Playlist>, I>>(base?: I): Playlist {
    return Playlist.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Playlist>, I>>(object: I): Playlist {
    const message = createBasePlaylist();
    message.id = object.id ?? "0";
    message.name = object.name ?? "";
    message.userId = object.userId ?? "0";
    message.videos = object.videos?.map((e) => Video.fromPartial(e)) || [];
    message.createdAt = object.createdAt ?? undefined;
    message.updatedAt = object.updatedAt ?? undefined;
    message.deletedAt = object.deletedAt ?? undefined;
    return message;
  },
};

function createBaseGetPlaylistByUserIdResponse(): GetPlaylistByUserIdResponse {
  return { playlists: [] };
}

export const GetPlaylistByUserIdResponse: MessageFns<GetPlaylistByUserIdResponse> = {
  encode(message: GetPlaylistByUserIdResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.playlists) {
      Playlist.encode(v!, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetPlaylistByUserIdResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetPlaylistByUserIdResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.playlists.push(Playlist.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GetPlaylistByUserIdResponse {
    return {
      playlists: globalThis.Array.isArray(object?.playlists)
        ? object.playlists.map((e: any) => Playlist.fromJSON(e))
        : [],
    };
  },

  toJSON(message: GetPlaylistByUserIdResponse): unknown {
    const obj: any = {};
    if (message.playlists?.length) {
      obj.playlists = message.playlists.map((e) => Playlist.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GetPlaylistByUserIdResponse>, I>>(base?: I): GetPlaylistByUserIdResponse {
    return GetPlaylistByUserIdResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GetPlaylistByUserIdResponse>, I>>(object: I): GetPlaylistByUserIdResponse {
    const message = createBaseGetPlaylistByUserIdResponse();
    message.playlists = object.playlists?.map((e) => Playlist.fromPartial(e)) || [];
    return message;
  },
};

function createBaseDeletePlaylistRequest(): DeletePlaylistRequest {
  return { id: "0" };
}

export const DeletePlaylistRequest: MessageFns<DeletePlaylistRequest> = {
  encode(message: DeletePlaylistRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.id !== "0") {
      writer.uint32(8).uint64(message.id);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): DeletePlaylistRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseDeletePlaylistRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.id = reader.uint64().toString();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): DeletePlaylistRequest {
    return { id: isSet(object.id) ? globalThis.String(object.id) : "0" };
  },

  toJSON(message: DeletePlaylistRequest): unknown {
    const obj: any = {};
    if (message.id !== "0") {
      obj.id = message.id;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<DeletePlaylistRequest>, I>>(base?: I): DeletePlaylistRequest {
    return DeletePlaylistRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<DeletePlaylistRequest>, I>>(object: I): DeletePlaylistRequest {
    const message = createBaseDeletePlaylistRequest();
    message.id = object.id ?? "0";
    return message;
  },
};

function createBaseDeletePlaylistResponse(): DeletePlaylistResponse {
  return { success: false };
}

export const DeletePlaylistResponse: MessageFns<DeletePlaylistResponse> = {
  encode(message: DeletePlaylistResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.success !== false) {
      writer.uint32(8).bool(message.success);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): DeletePlaylistResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseDeletePlaylistResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.success = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): DeletePlaylistResponse {
    return { success: isSet(object.success) ? globalThis.Boolean(object.success) : false };
  },

  toJSON(message: DeletePlaylistResponse): unknown {
    const obj: any = {};
    if (message.success !== false) {
      obj.success = message.success;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<DeletePlaylistResponse>, I>>(base?: I): DeletePlaylistResponse {
    return DeletePlaylistResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<DeletePlaylistResponse>, I>>(object: I): DeletePlaylistResponse {
    const message = createBaseDeletePlaylistResponse();
    message.success = object.success ?? false;
    return message;
  },
};

export interface PlaylistService {
  CreatePlaylist(
    request: DeepPartial<CreatePlaylistRequest>,
    metadata?: grpc.Metadata,
  ): Promise<CreatePlaylistResponse>;
  GetPlaylist(request: DeepPartial<GetPlaylistRequest>, metadata?: grpc.Metadata): Promise<Playlist>;
  GetPlaylistsByUserId(
    request: DeepPartial<GetPlaylistRequest>,
    metadata?: grpc.Metadata,
  ): Promise<GetPlaylistByUserIdResponse>;
  DeletePlaylist(
    request: DeepPartial<DeletePlaylistRequest>,
    metadata?: grpc.Metadata,
  ): Promise<DeletePlaylistResponse>;
}

export class PlaylistServiceClientImpl implements PlaylistService {
  private readonly rpc: Rpc;

  constructor(rpc: Rpc) {
    this.rpc = rpc;
    this.CreatePlaylist = this.CreatePlaylist.bind(this);
    this.GetPlaylist = this.GetPlaylist.bind(this);
    this.GetPlaylistsByUserId = this.GetPlaylistsByUserId.bind(this);
    this.DeletePlaylist = this.DeletePlaylist.bind(this);
  }

  CreatePlaylist(
    request: DeepPartial<CreatePlaylistRequest>,
    metadata?: grpc.Metadata,
  ): Promise<CreatePlaylistResponse> {
    return this.rpc.unary(PlaylistServiceCreatePlaylistDesc, CreatePlaylistRequest.fromPartial(request), metadata);
  }

  GetPlaylist(request: DeepPartial<GetPlaylistRequest>, metadata?: grpc.Metadata): Promise<Playlist> {
    return this.rpc.unary(PlaylistServiceGetPlaylistDesc, GetPlaylistRequest.fromPartial(request), metadata);
  }

  GetPlaylistsByUserId(
    request: DeepPartial<GetPlaylistRequest>,
    metadata?: grpc.Metadata,
  ): Promise<GetPlaylistByUserIdResponse> {
    return this.rpc.unary(PlaylistServiceGetPlaylistsByUserIdDesc, GetPlaylistRequest.fromPartial(request), metadata);
  }

  DeletePlaylist(
    request: DeepPartial<DeletePlaylistRequest>,
    metadata?: grpc.Metadata,
  ): Promise<DeletePlaylistResponse> {
    return this.rpc.unary(PlaylistServiceDeletePlaylistDesc, DeletePlaylistRequest.fromPartial(request), metadata);
  }
}

export const PlaylistServiceDesc = { serviceName: "playlist.PlaylistService" };

export const PlaylistServiceCreatePlaylistDesc: UnaryMethodDefinitionish = {
  methodName: "CreatePlaylist",
  service: PlaylistServiceDesc,
  requestStream: false,
  responseStream: false,
  requestType: {
    serializeBinary() {
      return CreatePlaylistRequest.encode(this).finish();
    },
  } as any,
  responseType: {
    deserializeBinary(data: Uint8Array) {
      const value = CreatePlaylistResponse.decode(data);
      return {
        ...value,
        toObject() {
          return value;
        },
      };
    },
  } as any,
};

export const PlaylistServiceGetPlaylistDesc: UnaryMethodDefinitionish = {
  methodName: "GetPlaylist",
  service: PlaylistServiceDesc,
  requestStream: false,
  responseStream: false,
  requestType: {
    serializeBinary() {
      return GetPlaylistRequest.encode(this).finish();
    },
  } as any,
  responseType: {
    deserializeBinary(data: Uint8Array) {
      const value = Playlist.decode(data);
      return {
        ...value,
        toObject() {
          return value;
        },
      };
    },
  } as any,
};

export const PlaylistServiceGetPlaylistsByUserIdDesc: UnaryMethodDefinitionish = {
  methodName: "GetPlaylistsByUserId",
  service: PlaylistServiceDesc,
  requestStream: false,
  responseStream: false,
  requestType: {
    serializeBinary() {
      return GetPlaylistRequest.encode(this).finish();
    },
  } as any,
  responseType: {
    deserializeBinary(data: Uint8Array) {
      const value = GetPlaylistByUserIdResponse.decode(data);
      return {
        ...value,
        toObject() {
          return value;
        },
      };
    },
  } as any,
};

export const PlaylistServiceDeletePlaylistDesc: UnaryMethodDefinitionish = {
  methodName: "DeletePlaylist",
  service: PlaylistServiceDesc,
  requestStream: false,
  responseStream: false,
  requestType: {
    serializeBinary() {
      return DeletePlaylistRequest.encode(this).finish();
    },
  } as any,
  responseType: {
    deserializeBinary(data: Uint8Array) {
      const value = DeletePlaylistResponse.decode(data);
      return {
        ...value,
        toObject() {
          return value;
        },
      };
    },
  } as any,
};

interface UnaryMethodDefinitionishR extends grpc.UnaryMethodDefinition<any, any> {
  requestStream: any;
  responseStream: any;
}

type UnaryMethodDefinitionish = UnaryMethodDefinitionishR;

interface Rpc {
  unary<T extends UnaryMethodDefinitionish>(
    methodDesc: T,
    request: any,
    metadata: grpc.Metadata | undefined,
  ): Promise<any>;
}

export class GrpcWebImpl {
  private host: string;
  private options: {
    transport?: grpc.TransportFactory;

    debug?: boolean;
    metadata?: grpc.Metadata;
    upStreamRetryCodes?: number[];
  };

  constructor(
    host: string,
    options: {
      transport?: grpc.TransportFactory;

      debug?: boolean;
      metadata?: grpc.Metadata;
      upStreamRetryCodes?: number[];
    },
  ) {
    this.host = host;
    this.options = options;
  }

  unary<T extends UnaryMethodDefinitionish>(
    methodDesc: T,
    _request: any,
    metadata: grpc.Metadata | undefined,
  ): Promise<any> {
    const request = { ..._request, ...methodDesc.requestType };
    const maybeCombinedMetadata = metadata && this.options.metadata
      ? new BrowserHeaders({ ...this.options?.metadata.headersMap, ...metadata?.headersMap })
      : metadata ?? this.options.metadata;
    return new Promise((resolve, reject) => {
      grpc.unary(methodDesc, {
        request,
        host: this.host,
        metadata: maybeCombinedMetadata ?? {},
        ...(this.options.transport !== undefined ? { transport: this.options.transport } : {}),
        debug: this.options.debug ?? false,
        onEnd: function (response) {
          if (response.status === grpc.Code.OK) {
            resolve(response.message!.toObject());
          } else {
            const err = new GrpcWebError(response.statusMessage, response.status, response.trailers);
            reject(err);
          }
        },
      });
    });
  }
}

function bytesFromBase64(b64: string): Uint8Array {
  if ((globalThis as any).Buffer) {
    return Uint8Array.from(globalThis.Buffer.from(b64, "base64"));
  } else {
    const bin = globalThis.atob(b64);
    const arr = new Uint8Array(bin.length);
    for (let i = 0; i < bin.length; ++i) {
      arr[i] = bin.charCodeAt(i);
    }
    return arr;
  }
}

function base64FromBytes(arr: Uint8Array): string {
  if ((globalThis as any).Buffer) {
    return globalThis.Buffer.from(arr).toString("base64");
  } else {
    const bin: string[] = [];
    arr.forEach((byte) => {
      bin.push(globalThis.String.fromCharCode(byte));
    });
    return globalThis.btoa(bin.join(""));
  }
}

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function toTimestamp(date: Date): Timestamp {
  const seconds = Math.trunc(date.getTime() / 1_000).toString();
  const nanos = (date.getTime() % 1_000) * 1_000_000;
  return { seconds, nanos };
}

function fromTimestamp(t: Timestamp): Date {
  let millis = (globalThis.Number(t.seconds) || 0) * 1_000;
  millis += (t.nanos || 0) / 1_000_000;
  return new globalThis.Date(millis);
}

function fromJsonTimestamp(o: any): Date {
  if (o instanceof globalThis.Date) {
    return o;
  } else if (typeof o === "string") {
    return new globalThis.Date(o);
  } else {
    return fromTimestamp(Timestamp.fromJSON(o));
  }
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export class GrpcWebError extends globalThis.Error {
  constructor(message: string, public code: grpc.Code, public metadata: grpc.Metadata) {
    super(message);
  }
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
