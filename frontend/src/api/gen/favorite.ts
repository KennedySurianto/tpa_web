// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.7.0
//   protoc               v6.31.0
// source: favorite.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { grpc } from "@improbable-eng/grpc-web";
import { BrowserHeaders } from "browser-headers";
import { Timestamp } from "./google/protobuf/timestamp";
import { Video } from "./video";

export const protobufPackage = "favorite";

export interface Favorite {
  id: string;
  userId: string;
  videoId: string;
  createdAt?: Date | undefined;
}

export interface AddFavoriteRequest {
  userId: string;
  videoId: string;
}

export interface AddFavoriteResponse {
  success: boolean;
}

export interface RemoveFavoriteRequest {
  userId: string;
  videoId: string;
}

export interface RemoveFavoriteResponse {
  success: boolean;
}

export interface GetFavoriteVideosByUserIdRequest {
  userId: string;
  /** To check for video access permissions */
  currentUserId: string;
}

export interface GetFavoriteVideosByUserIdResponse {
  videos: Video[];
}

export interface IsFavoritedRequest {
  userId: string;
  videoId: string;
}

export interface IsFavoritedResponse {
  isFavorited: boolean;
}

function createBaseFavorite(): Favorite {
  return { id: "0", userId: "0", videoId: "0", createdAt: undefined };
}

export const Favorite: MessageFns<Favorite> = {
  encode(message: Favorite, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.id !== "0") {
      writer.uint32(8).uint64(message.id);
    }
    if (message.userId !== "0") {
      writer.uint32(16).uint64(message.userId);
    }
    if (message.videoId !== "0") {
      writer.uint32(24).uint64(message.videoId);
    }
    if (message.createdAt !== undefined) {
      Timestamp.encode(toTimestamp(message.createdAt), writer.uint32(34).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Favorite {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseFavorite();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.id = reader.uint64().toString();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.userId = reader.uint64().toString();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.videoId = reader.uint64().toString();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.createdAt = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Favorite {
    return {
      id: isSet(object.id) ? globalThis.String(object.id) : "0",
      userId: isSet(object.userId) ? globalThis.String(object.userId) : "0",
      videoId: isSet(object.videoId) ? globalThis.String(object.videoId) : "0",
      createdAt: isSet(object.createdAt) ? fromJsonTimestamp(object.createdAt) : undefined,
    };
  },

  toJSON(message: Favorite): unknown {
    const obj: any = {};
    if (message.id !== "0") {
      obj.id = message.id;
    }
    if (message.userId !== "0") {
      obj.userId = message.userId;
    }
    if (message.videoId !== "0") {
      obj.videoId = message.videoId;
    }
    if (message.createdAt !== undefined) {
      obj.createdAt = message.createdAt.toISOString();
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<Favorite>, I>>(base?: I): Favorite {
    return Favorite.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Favorite>, I>>(object: I): Favorite {
    const message = createBaseFavorite();
    message.id = object.id ?? "0";
    message.userId = object.userId ?? "0";
    message.videoId = object.videoId ?? "0";
    message.createdAt = object.createdAt ?? undefined;
    return message;
  },
};

function createBaseAddFavoriteRequest(): AddFavoriteRequest {
  return { userId: "0", videoId: "0" };
}

export const AddFavoriteRequest: MessageFns<AddFavoriteRequest> = {
  encode(message: AddFavoriteRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.userId !== "0") {
      writer.uint32(8).uint64(message.userId);
    }
    if (message.videoId !== "0") {
      writer.uint32(16).uint64(message.videoId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): AddFavoriteRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseAddFavoriteRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.userId = reader.uint64().toString();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.videoId = reader.uint64().toString();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): AddFavoriteRequest {
    return {
      userId: isSet(object.userId) ? globalThis.String(object.userId) : "0",
      videoId: isSet(object.videoId) ? globalThis.String(object.videoId) : "0",
    };
  },

  toJSON(message: AddFavoriteRequest): unknown {
    const obj: any = {};
    if (message.userId !== "0") {
      obj.userId = message.userId;
    }
    if (message.videoId !== "0") {
      obj.videoId = message.videoId;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<AddFavoriteRequest>, I>>(base?: I): AddFavoriteRequest {
    return AddFavoriteRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<AddFavoriteRequest>, I>>(object: I): AddFavoriteRequest {
    const message = createBaseAddFavoriteRequest();
    message.userId = object.userId ?? "0";
    message.videoId = object.videoId ?? "0";
    return message;
  },
};

function createBaseAddFavoriteResponse(): AddFavoriteResponse {
  return { success: false };
}

export const AddFavoriteResponse: MessageFns<AddFavoriteResponse> = {
  encode(message: AddFavoriteResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.success !== false) {
      writer.uint32(8).bool(message.success);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): AddFavoriteResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseAddFavoriteResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.success = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): AddFavoriteResponse {
    return { success: isSet(object.success) ? globalThis.Boolean(object.success) : false };
  },

  toJSON(message: AddFavoriteResponse): unknown {
    const obj: any = {};
    if (message.success !== false) {
      obj.success = message.success;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<AddFavoriteResponse>, I>>(base?: I): AddFavoriteResponse {
    return AddFavoriteResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<AddFavoriteResponse>, I>>(object: I): AddFavoriteResponse {
    const message = createBaseAddFavoriteResponse();
    message.success = object.success ?? false;
    return message;
  },
};

function createBaseRemoveFavoriteRequest(): RemoveFavoriteRequest {
  return { userId: "0", videoId: "0" };
}

export const RemoveFavoriteRequest: MessageFns<RemoveFavoriteRequest> = {
  encode(message: RemoveFavoriteRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.userId !== "0") {
      writer.uint32(8).uint64(message.userId);
    }
    if (message.videoId !== "0") {
      writer.uint32(16).uint64(message.videoId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): RemoveFavoriteRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseRemoveFavoriteRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.userId = reader.uint64().toString();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.videoId = reader.uint64().toString();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): RemoveFavoriteRequest {
    return {
      userId: isSet(object.userId) ? globalThis.String(object.userId) : "0",
      videoId: isSet(object.videoId) ? globalThis.String(object.videoId) : "0",
    };
  },

  toJSON(message: RemoveFavoriteRequest): unknown {
    const obj: any = {};
    if (message.userId !== "0") {
      obj.userId = message.userId;
    }
    if (message.videoId !== "0") {
      obj.videoId = message.videoId;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<RemoveFavoriteRequest>, I>>(base?: I): RemoveFavoriteRequest {
    return RemoveFavoriteRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<RemoveFavoriteRequest>, I>>(object: I): RemoveFavoriteRequest {
    const message = createBaseRemoveFavoriteRequest();
    message.userId = object.userId ?? "0";
    message.videoId = object.videoId ?? "0";
    return message;
  },
};

function createBaseRemoveFavoriteResponse(): RemoveFavoriteResponse {
  return { success: false };
}

export const RemoveFavoriteResponse: MessageFns<RemoveFavoriteResponse> = {
  encode(message: RemoveFavoriteResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.success !== false) {
      writer.uint32(8).bool(message.success);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): RemoveFavoriteResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseRemoveFavoriteResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.success = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): RemoveFavoriteResponse {
    return { success: isSet(object.success) ? globalThis.Boolean(object.success) : false };
  },

  toJSON(message: RemoveFavoriteResponse): unknown {
    const obj: any = {};
    if (message.success !== false) {
      obj.success = message.success;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<RemoveFavoriteResponse>, I>>(base?: I): RemoveFavoriteResponse {
    return RemoveFavoriteResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<RemoveFavoriteResponse>, I>>(object: I): RemoveFavoriteResponse {
    const message = createBaseRemoveFavoriteResponse();
    message.success = object.success ?? false;
    return message;
  },
};

function createBaseGetFavoriteVideosByUserIdRequest(): GetFavoriteVideosByUserIdRequest {
  return { userId: "0", currentUserId: "0" };
}

export const GetFavoriteVideosByUserIdRequest: MessageFns<GetFavoriteVideosByUserIdRequest> = {
  encode(message: GetFavoriteVideosByUserIdRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.userId !== "0") {
      writer.uint32(8).uint64(message.userId);
    }
    if (message.currentUserId !== "0") {
      writer.uint32(16).uint64(message.currentUserId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetFavoriteVideosByUserIdRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetFavoriteVideosByUserIdRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.userId = reader.uint64().toString();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.currentUserId = reader.uint64().toString();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GetFavoriteVideosByUserIdRequest {
    return {
      userId: isSet(object.userId) ? globalThis.String(object.userId) : "0",
      currentUserId: isSet(object.currentUserId) ? globalThis.String(object.currentUserId) : "0",
    };
  },

  toJSON(message: GetFavoriteVideosByUserIdRequest): unknown {
    const obj: any = {};
    if (message.userId !== "0") {
      obj.userId = message.userId;
    }
    if (message.currentUserId !== "0") {
      obj.currentUserId = message.currentUserId;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GetFavoriteVideosByUserIdRequest>, I>>(
    base?: I,
  ): GetFavoriteVideosByUserIdRequest {
    return GetFavoriteVideosByUserIdRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GetFavoriteVideosByUserIdRequest>, I>>(
    object: I,
  ): GetFavoriteVideosByUserIdRequest {
    const message = createBaseGetFavoriteVideosByUserIdRequest();
    message.userId = object.userId ?? "0";
    message.currentUserId = object.currentUserId ?? "0";
    return message;
  },
};

function createBaseGetFavoriteVideosByUserIdResponse(): GetFavoriteVideosByUserIdResponse {
  return { videos: [] };
}

export const GetFavoriteVideosByUserIdResponse: MessageFns<GetFavoriteVideosByUserIdResponse> = {
  encode(message: GetFavoriteVideosByUserIdResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.videos) {
      Video.encode(v!, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetFavoriteVideosByUserIdResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetFavoriteVideosByUserIdResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.videos.push(Video.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GetFavoriteVideosByUserIdResponse {
    return { videos: globalThis.Array.isArray(object?.videos) ? object.videos.map((e: any) => Video.fromJSON(e)) : [] };
  },

  toJSON(message: GetFavoriteVideosByUserIdResponse): unknown {
    const obj: any = {};
    if (message.videos?.length) {
      obj.videos = message.videos.map((e) => Video.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GetFavoriteVideosByUserIdResponse>, I>>(
    base?: I,
  ): GetFavoriteVideosByUserIdResponse {
    return GetFavoriteVideosByUserIdResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GetFavoriteVideosByUserIdResponse>, I>>(
    object: I,
  ): GetFavoriteVideosByUserIdResponse {
    const message = createBaseGetFavoriteVideosByUserIdResponse();
    message.videos = object.videos?.map((e) => Video.fromPartial(e)) || [];
    return message;
  },
};

function createBaseIsFavoritedRequest(): IsFavoritedRequest {
  return { userId: "0", videoId: "0" };
}

export const IsFavoritedRequest: MessageFns<IsFavoritedRequest> = {
  encode(message: IsFavoritedRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.userId !== "0") {
      writer.uint32(8).uint64(message.userId);
    }
    if (message.videoId !== "0") {
      writer.uint32(16).uint64(message.videoId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): IsFavoritedRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseIsFavoritedRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.userId = reader.uint64().toString();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.videoId = reader.uint64().toString();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): IsFavoritedRequest {
    return {
      userId: isSet(object.userId) ? globalThis.String(object.userId) : "0",
      videoId: isSet(object.videoId) ? globalThis.String(object.videoId) : "0",
    };
  },

  toJSON(message: IsFavoritedRequest): unknown {
    const obj: any = {};
    if (message.userId !== "0") {
      obj.userId = message.userId;
    }
    if (message.videoId !== "0") {
      obj.videoId = message.videoId;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<IsFavoritedRequest>, I>>(base?: I): IsFavoritedRequest {
    return IsFavoritedRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<IsFavoritedRequest>, I>>(object: I): IsFavoritedRequest {
    const message = createBaseIsFavoritedRequest();
    message.userId = object.userId ?? "0";
    message.videoId = object.videoId ?? "0";
    return message;
  },
};

function createBaseIsFavoritedResponse(): IsFavoritedResponse {
  return { isFavorited: false };
}

export const IsFavoritedResponse: MessageFns<IsFavoritedResponse> = {
  encode(message: IsFavoritedResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.isFavorited !== false) {
      writer.uint32(8).bool(message.isFavorited);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): IsFavoritedResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseIsFavoritedResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.isFavorited = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): IsFavoritedResponse {
    return { isFavorited: isSet(object.isFavorited) ? globalThis.Boolean(object.isFavorited) : false };
  },

  toJSON(message: IsFavoritedResponse): unknown {
    const obj: any = {};
    if (message.isFavorited !== false) {
      obj.isFavorited = message.isFavorited;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<IsFavoritedResponse>, I>>(base?: I): IsFavoritedResponse {
    return IsFavoritedResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<IsFavoritedResponse>, I>>(object: I): IsFavoritedResponse {
    const message = createBaseIsFavoritedResponse();
    message.isFavorited = object.isFavorited ?? false;
    return message;
  },
};

export interface FavoriteService {
  AddFavorite(request: DeepPartial<AddFavoriteRequest>, metadata?: grpc.Metadata): Promise<AddFavoriteResponse>;
  RemoveFavorite(
    request: DeepPartial<RemoveFavoriteRequest>,
    metadata?: grpc.Metadata,
  ): Promise<RemoveFavoriteResponse>;
  GetFavoriteVideosByUserId(
    request: DeepPartial<GetFavoriteVideosByUserIdRequest>,
    metadata?: grpc.Metadata,
  ): Promise<GetFavoriteVideosByUserIdResponse>;
  IsFavorited(request: DeepPartial<IsFavoritedRequest>, metadata?: grpc.Metadata): Promise<IsFavoritedResponse>;
}

export class FavoriteServiceClientImpl implements FavoriteService {
  private readonly rpc: Rpc;

  constructor(rpc: Rpc) {
    this.rpc = rpc;
    this.AddFavorite = this.AddFavorite.bind(this);
    this.RemoveFavorite = this.RemoveFavorite.bind(this);
    this.GetFavoriteVideosByUserId = this.GetFavoriteVideosByUserId.bind(this);
    this.IsFavorited = this.IsFavorited.bind(this);
  }

  AddFavorite(request: DeepPartial<AddFavoriteRequest>, metadata?: grpc.Metadata): Promise<AddFavoriteResponse> {
    return this.rpc.unary(FavoriteServiceAddFavoriteDesc, AddFavoriteRequest.fromPartial(request), metadata);
  }

  RemoveFavorite(
    request: DeepPartial<RemoveFavoriteRequest>,
    metadata?: grpc.Metadata,
  ): Promise<RemoveFavoriteResponse> {
    return this.rpc.unary(FavoriteServiceRemoveFavoriteDesc, RemoveFavoriteRequest.fromPartial(request), metadata);
  }

  GetFavoriteVideosByUserId(
    request: DeepPartial<GetFavoriteVideosByUserIdRequest>,
    metadata?: grpc.Metadata,
  ): Promise<GetFavoriteVideosByUserIdResponse> {
    return this.rpc.unary(
      FavoriteServiceGetFavoriteVideosByUserIdDesc,
      GetFavoriteVideosByUserIdRequest.fromPartial(request),
      metadata,
    );
  }

  IsFavorited(request: DeepPartial<IsFavoritedRequest>, metadata?: grpc.Metadata): Promise<IsFavoritedResponse> {
    return this.rpc.unary(FavoriteServiceIsFavoritedDesc, IsFavoritedRequest.fromPartial(request), metadata);
  }
}

export const FavoriteServiceDesc = { serviceName: "favorite.FavoriteService" };

export const FavoriteServiceAddFavoriteDesc: UnaryMethodDefinitionish = {
  methodName: "AddFavorite",
  service: FavoriteServiceDesc,
  requestStream: false,
  responseStream: false,
  requestType: {
    serializeBinary() {
      return AddFavoriteRequest.encode(this).finish();
    },
  } as any,
  responseType: {
    deserializeBinary(data: Uint8Array) {
      const value = AddFavoriteResponse.decode(data);
      return {
        ...value,
        toObject() {
          return value;
        },
      };
    },
  } as any,
};

export const FavoriteServiceRemoveFavoriteDesc: UnaryMethodDefinitionish = {
  methodName: "RemoveFavorite",
  service: FavoriteServiceDesc,
  requestStream: false,
  responseStream: false,
  requestType: {
    serializeBinary() {
      return RemoveFavoriteRequest.encode(this).finish();
    },
  } as any,
  responseType: {
    deserializeBinary(data: Uint8Array) {
      const value = RemoveFavoriteResponse.decode(data);
      return {
        ...value,
        toObject() {
          return value;
        },
      };
    },
  } as any,
};

export const FavoriteServiceGetFavoriteVideosByUserIdDesc: UnaryMethodDefinitionish = {
  methodName: "GetFavoriteVideosByUserId",
  service: FavoriteServiceDesc,
  requestStream: false,
  responseStream: false,
  requestType: {
    serializeBinary() {
      return GetFavoriteVideosByUserIdRequest.encode(this).finish();
    },
  } as any,
  responseType: {
    deserializeBinary(data: Uint8Array) {
      const value = GetFavoriteVideosByUserIdResponse.decode(data);
      return {
        ...value,
        toObject() {
          return value;
        },
      };
    },
  } as any,
};

export const FavoriteServiceIsFavoritedDesc: UnaryMethodDefinitionish = {
  methodName: "IsFavorited",
  service: FavoriteServiceDesc,
  requestStream: false,
  responseStream: false,
  requestType: {
    serializeBinary() {
      return IsFavoritedRequest.encode(this).finish();
    },
  } as any,
  responseType: {
    deserializeBinary(data: Uint8Array) {
      const value = IsFavoritedResponse.decode(data);
      return {
        ...value,
        toObject() {
          return value;
        },
      };
    },
  } as any,
};

interface UnaryMethodDefinitionishR extends grpc.UnaryMethodDefinition<any, any> {
  requestStream: any;
  responseStream: any;
}

type UnaryMethodDefinitionish = UnaryMethodDefinitionishR;

interface Rpc {
  unary<T extends UnaryMethodDefinitionish>(
    methodDesc: T,
    request: any,
    metadata: grpc.Metadata | undefined,
  ): Promise<any>;
}

export class GrpcWebImpl {
  private host: string;
  private options: {
    transport?: grpc.TransportFactory;

    debug?: boolean;
    metadata?: grpc.Metadata;
    upStreamRetryCodes?: number[];
  };

  constructor(
    host: string,
    options: {
      transport?: grpc.TransportFactory;

      debug?: boolean;
      metadata?: grpc.Metadata;
      upStreamRetryCodes?: number[];
    },
  ) {
    this.host = host;
    this.options = options;
  }

  unary<T extends UnaryMethodDefinitionish>(
    methodDesc: T,
    _request: any,
    metadata: grpc.Metadata | undefined,
  ): Promise<any> {
    const request = { ..._request, ...methodDesc.requestType };
    const maybeCombinedMetadata = metadata && this.options.metadata
      ? new BrowserHeaders({ ...this.options?.metadata.headersMap, ...metadata?.headersMap })
      : metadata ?? this.options.metadata;
    return new Promise((resolve, reject) => {
      grpc.unary(methodDesc, {
        request,
        host: this.host,
        metadata: maybeCombinedMetadata ?? {},
        ...(this.options.transport !== undefined ? { transport: this.options.transport } : {}),
        debug: this.options.debug ?? false,
        onEnd: function (response) {
          if (response.status === grpc.Code.OK) {
            resolve(response.message!.toObject());
          } else {
            const err = new GrpcWebError(response.statusMessage, response.status, response.trailers);
            reject(err);
          }
        },
      });
    });
  }
}

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function toTimestamp(date: Date): Timestamp {
  const seconds = Math.trunc(date.getTime() / 1_000).toString();
  const nanos = (date.getTime() % 1_000) * 1_000_000;
  return { seconds, nanos };
}

function fromTimestamp(t: Timestamp): Date {
  let millis = (globalThis.Number(t.seconds) || 0) * 1_000;
  millis += (t.nanos || 0) / 1_000_000;
  return new globalThis.Date(millis);
}

function fromJsonTimestamp(o: any): Date {
  if (o instanceof globalThis.Date) {
    return o;
  } else if (typeof o === "string") {
    return new globalThis.Date(o);
  } else {
    return fromTimestamp(Timestamp.fromJSON(o));
  }
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export class GrpcWebError extends globalThis.Error {
  constructor(message: string, public code: grpc.Code, public metadata: grpc.Metadata) {
    super(message);
  }
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
