// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.7.0
//   protoc               v6.31.0
// source: follow.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { grpc } from "@improbable-eng/grpc-web";
import { BrowserHeaders } from "browser-headers";

export const protobufPackage = "activity";

export interface FollowRequest {
  followerId: number;
  followedId: number;
}

export interface UserRequest {
  userId: number;
}

export interface FollowItem {
  followerId: number;
  followedId: number;
}

export interface FollowList {
  follows: FollowItem[];
}

export interface Empty {
}

function createBaseFollowRequest(): FollowRequest {
  return { followerId: 0, followedId: 0 };
}

export const FollowRequest: MessageFns<FollowRequest> = {
  encode(message: FollowRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.followerId !== 0) {
      writer.uint32(8).uint32(message.followerId);
    }
    if (message.followedId !== 0) {
      writer.uint32(16).uint32(message.followedId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): FollowRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseFollowRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.followerId = reader.uint32();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.followedId = reader.uint32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): FollowRequest {
    return {
      followerId: isSet(object.followerId) ? globalThis.Number(object.followerId) : 0,
      followedId: isSet(object.followedId) ? globalThis.Number(object.followedId) : 0,
    };
  },

  toJSON(message: FollowRequest): unknown {
    const obj: any = {};
    if (message.followerId !== 0) {
      obj.followerId = Math.round(message.followerId);
    }
    if (message.followedId !== 0) {
      obj.followedId = Math.round(message.followedId);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<FollowRequest>, I>>(base?: I): FollowRequest {
    return FollowRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<FollowRequest>, I>>(object: I): FollowRequest {
    const message = createBaseFollowRequest();
    message.followerId = object.followerId ?? 0;
    message.followedId = object.followedId ?? 0;
    return message;
  },
};

function createBaseUserRequest(): UserRequest {
  return { userId: 0 };
}

export const UserRequest: MessageFns<UserRequest> = {
  encode(message: UserRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.userId !== 0) {
      writer.uint32(8).uint32(message.userId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): UserRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseUserRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.userId = reader.uint32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): UserRequest {
    return { userId: isSet(object.userId) ? globalThis.Number(object.userId) : 0 };
  },

  toJSON(message: UserRequest): unknown {
    const obj: any = {};
    if (message.userId !== 0) {
      obj.userId = Math.round(message.userId);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<UserRequest>, I>>(base?: I): UserRequest {
    return UserRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<UserRequest>, I>>(object: I): UserRequest {
    const message = createBaseUserRequest();
    message.userId = object.userId ?? 0;
    return message;
  },
};

function createBaseFollowItem(): FollowItem {
  return { followerId: 0, followedId: 0 };
}

export const FollowItem: MessageFns<FollowItem> = {
  encode(message: FollowItem, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.followerId !== 0) {
      writer.uint32(8).uint32(message.followerId);
    }
    if (message.followedId !== 0) {
      writer.uint32(16).uint32(message.followedId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): FollowItem {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseFollowItem();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.followerId = reader.uint32();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.followedId = reader.uint32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): FollowItem {
    return {
      followerId: isSet(object.followerId) ? globalThis.Number(object.followerId) : 0,
      followedId: isSet(object.followedId) ? globalThis.Number(object.followedId) : 0,
    };
  },

  toJSON(message: FollowItem): unknown {
    const obj: any = {};
    if (message.followerId !== 0) {
      obj.followerId = Math.round(message.followerId);
    }
    if (message.followedId !== 0) {
      obj.followedId = Math.round(message.followedId);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<FollowItem>, I>>(base?: I): FollowItem {
    return FollowItem.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<FollowItem>, I>>(object: I): FollowItem {
    const message = createBaseFollowItem();
    message.followerId = object.followerId ?? 0;
    message.followedId = object.followedId ?? 0;
    return message;
  },
};

function createBaseFollowList(): FollowList {
  return { follows: [] };
}

export const FollowList: MessageFns<FollowList> = {
  encode(message: FollowList, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.follows) {
      FollowItem.encode(v!, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): FollowList {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseFollowList();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.follows.push(FollowItem.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): FollowList {
    return {
      follows: globalThis.Array.isArray(object?.follows) ? object.follows.map((e: any) => FollowItem.fromJSON(e)) : [],
    };
  },

  toJSON(message: FollowList): unknown {
    const obj: any = {};
    if (message.follows?.length) {
      obj.follows = message.follows.map((e) => FollowItem.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<FollowList>, I>>(base?: I): FollowList {
    return FollowList.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<FollowList>, I>>(object: I): FollowList {
    const message = createBaseFollowList();
    message.follows = object.follows?.map((e) => FollowItem.fromPartial(e)) || [];
    return message;
  },
};

function createBaseEmpty(): Empty {
  return {};
}

export const Empty: MessageFns<Empty> = {
  encode(_: Empty, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Empty {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseEmpty();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): Empty {
    return {};
  },

  toJSON(_: Empty): unknown {
    const obj: any = {};
    return obj;
  },

  create<I extends Exact<DeepPartial<Empty>, I>>(base?: I): Empty {
    return Empty.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Empty>, I>>(_: I): Empty {
    const message = createBaseEmpty();
    return message;
  },
};

export interface FollowService {
  Follow(request: DeepPartial<FollowRequest>, metadata?: grpc.Metadata): Promise<Empty>;
  Unfollow(request: DeepPartial<FollowRequest>, metadata?: grpc.Metadata): Promise<Empty>;
  GetFollowers(request: DeepPartial<UserRequest>, metadata?: grpc.Metadata): Promise<FollowList>;
  GetFollowing(request: DeepPartial<UserRequest>, metadata?: grpc.Metadata): Promise<FollowList>;
}

export class FollowServiceClientImpl implements FollowService {
  private readonly rpc: Rpc;

  constructor(rpc: Rpc) {
    this.rpc = rpc;
    this.Follow = this.Follow.bind(this);
    this.Unfollow = this.Unfollow.bind(this);
    this.GetFollowers = this.GetFollowers.bind(this);
    this.GetFollowing = this.GetFollowing.bind(this);
  }

  Follow(request: DeepPartial<FollowRequest>, metadata?: grpc.Metadata): Promise<Empty> {
    return this.rpc.unary(FollowServiceFollowDesc, FollowRequest.fromPartial(request), metadata);
  }

  Unfollow(request: DeepPartial<FollowRequest>, metadata?: grpc.Metadata): Promise<Empty> {
    return this.rpc.unary(FollowServiceUnfollowDesc, FollowRequest.fromPartial(request), metadata);
  }

  GetFollowers(request: DeepPartial<UserRequest>, metadata?: grpc.Metadata): Promise<FollowList> {
    return this.rpc.unary(FollowServiceGetFollowersDesc, UserRequest.fromPartial(request), metadata);
  }

  GetFollowing(request: DeepPartial<UserRequest>, metadata?: grpc.Metadata): Promise<FollowList> {
    return this.rpc.unary(FollowServiceGetFollowingDesc, UserRequest.fromPartial(request), metadata);
  }
}

export const FollowServiceDesc = { serviceName: "activity.FollowService" };

export const FollowServiceFollowDesc: UnaryMethodDefinitionish = {
  methodName: "Follow",
  service: FollowServiceDesc,
  requestStream: false,
  responseStream: false,
  requestType: {
    serializeBinary() {
      return FollowRequest.encode(this).finish();
    },
  } as any,
  responseType: {
    deserializeBinary(data: Uint8Array) {
      const value = Empty.decode(data);
      return {
        ...value,
        toObject() {
          return value;
        },
      };
    },
  } as any,
};

export const FollowServiceUnfollowDesc: UnaryMethodDefinitionish = {
  methodName: "Unfollow",
  service: FollowServiceDesc,
  requestStream: false,
  responseStream: false,
  requestType: {
    serializeBinary() {
      return FollowRequest.encode(this).finish();
    },
  } as any,
  responseType: {
    deserializeBinary(data: Uint8Array) {
      const value = Empty.decode(data);
      return {
        ...value,
        toObject() {
          return value;
        },
      };
    },
  } as any,
};

export const FollowServiceGetFollowersDesc: UnaryMethodDefinitionish = {
  methodName: "GetFollowers",
  service: FollowServiceDesc,
  requestStream: false,
  responseStream: false,
  requestType: {
    serializeBinary() {
      return UserRequest.encode(this).finish();
    },
  } as any,
  responseType: {
    deserializeBinary(data: Uint8Array) {
      const value = FollowList.decode(data);
      return {
        ...value,
        toObject() {
          return value;
        },
      };
    },
  } as any,
};

export const FollowServiceGetFollowingDesc: UnaryMethodDefinitionish = {
  methodName: "GetFollowing",
  service: FollowServiceDesc,
  requestStream: false,
  responseStream: false,
  requestType: {
    serializeBinary() {
      return UserRequest.encode(this).finish();
    },
  } as any,
  responseType: {
    deserializeBinary(data: Uint8Array) {
      const value = FollowList.decode(data);
      return {
        ...value,
        toObject() {
          return value;
        },
      };
    },
  } as any,
};

interface UnaryMethodDefinitionishR extends grpc.UnaryMethodDefinition<any, any> {
  requestStream: any;
  responseStream: any;
}

type UnaryMethodDefinitionish = UnaryMethodDefinitionishR;

interface Rpc {
  unary<T extends UnaryMethodDefinitionish>(
    methodDesc: T,
    request: any,
    metadata: grpc.Metadata | undefined,
  ): Promise<any>;
}

export class GrpcWebImpl {
  private host: string;
  private options: {
    transport?: grpc.TransportFactory;

    debug?: boolean;
    metadata?: grpc.Metadata;
    upStreamRetryCodes?: number[];
  };

  constructor(
    host: string,
    options: {
      transport?: grpc.TransportFactory;

      debug?: boolean;
      metadata?: grpc.Metadata;
      upStreamRetryCodes?: number[];
    },
  ) {
    this.host = host;
    this.options = options;
  }

  unary<T extends UnaryMethodDefinitionish>(
    methodDesc: T,
    _request: any,
    metadata: grpc.Metadata | undefined,
  ): Promise<any> {
    const request = { ..._request, ...methodDesc.requestType };
    const maybeCombinedMetadata = metadata && this.options.metadata
      ? new BrowserHeaders({ ...this.options?.metadata.headersMap, ...metadata?.headersMap })
      : metadata ?? this.options.metadata;
    return new Promise((resolve, reject) => {
      grpc.unary(methodDesc, {
        request,
        host: this.host,
        metadata: maybeCombinedMetadata ?? {},
        ...(this.options.transport !== undefined ? { transport: this.options.transport } : {}),
        debug: this.options.debug ?? false,
        onEnd: function (response) {
          if (response.status === grpc.Code.OK) {
            resolve(response.message!.toObject());
          } else {
            const err = new GrpcWebError(response.statusMessage, response.status, response.trailers);
            reject(err);
          }
        },
      });
    });
  }
}

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export class GrpcWebError extends globalThis.Error {
  constructor(message: string, public code: grpc.Code, public metadata: grpc.Metadata) {
    super(message);
  }
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
